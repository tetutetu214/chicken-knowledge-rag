# 監査 2026-07-18（Fable 最終監査・audit-20260712 の更新版）

Fable サブスク枠の最終日に実施した更新監査。以降は Opus/Sonnet + `/fable` skill 運用。

## 現状（2026-07-18 実測）

- audit-20260712 以降の新規コミットは docs 2件のみ（`b70e682`, `a51c3a3`）。コード変更・PR・Issue の動きはゼロで、「aws login 待ちで所見3が止まっている」状態が6日間継続。

## 所見の再判定

- **所見1: Issue #1 は誤起票ではない → backlog 維持 — 反映済み・判断有効**（todo.md に記載済み、Issue #1 は OPEN のまま変化なし）。
- **所見2: query-expansion ブランチは捨て、モジュールは拾う — 未着手のまま有効**（`feature/query-expansion-issue-31` は方針どおり保持、cherry-pick 未実施）。
- **所見3: Scheduler 記述の書き分け修正（要 aws login） — 未着手のまま有効**（todo.md:232/301 の記述・チェック状態に変化なし。実リソース照合は次回 aws login 後）。

## 新規所見（2026-07-18）

- **所見4（高・公開リポジトリのため要注意）: `docs/photo/` に未追跡画像2枚**（IMG_1602.PNG / IMG_1603.PNG、2026-05-20 作成）。本リポジトリはパブリックのため、**羽数・小屋・市町村が写り込んでいないか本人確認が済むまでコミット禁止**（CLAUDE.md「飼育地・規模情報の取り扱い」参照）。確認後、コミットするか削除／gitignore するかを判断する。
- **所見5（低）: `AGENTS.md`（symlink）が git 管理外のまま**。Codex 委譲前に chore コミット。
- **所見6（低）: マージ済み featureブランチ6本が残骸として残存**（insights-dashboard / issue-30 / issue-31 / issue-32 / passkey-backend / passkey-frontend。todo.md 上は全て PR マージ済み記載）。削除前に各 PR のマージ状態を照合してから `git branch -d` + `git push origin --delete`。

## 次の一手（優先順）

1. ⏳てつてつ: aws login 後に Scheduler 記述の書き分け修正（実リソース確認とセット、所見3）
2. ⏳てつてつ: docs/photo/ の画像2枚の中身確認（公開リポのためコミット可否判断。所見4）
3. 家族体感系の着手判断（👎+自由記述 / クエリ拡張の再適用 / Phase3 分類）
4. 雑務: AGENTS.md chore コミット + マージ済みブランチ6本の照合・掃除
