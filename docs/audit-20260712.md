# 監査 2026-07-12（Fable・07-07 運用健康診断の更新版）

07-06 以降コード変更なし（docs コミット2件のみ）。懸案3件（Issue #1 / query-expansion ブランチ / Scheduler 記述矛盾）を実測して判断を確定する。

## 所見1（前提訂正）: Issue #1 は誤起票ではない

todo.md は「別リポジトリ内容が混入した誤起票」としていたが、実測では **Issue #1（anime-kit 導入 — insights の数値 countUp）は本リポジトリの `web/app/insights/page.tsx` を対象にした正当な機能提案**だった（本文が anime-kit を「導入する」側として明記。2026-06-13 起票・enhancement ラベル・comments 0）。

- 判断: 「誤起票としてクローズ」は不要。純粋な優先度判断に切り替える。UI 装飾系であり、家族体感系（👎フィードバック・クエリ拡張）より優先度は低い。着手しないなら open のまま backlog 扱いで問題ない。

## 所見2（判断）: feature/query-expansion-issue-31 — ブランチは捨て、モジュールは拾う

- 実測: main 未マージ3コミット・756 insertions（うち `queryExpansion.ts` 純関数モジュール 281 行）、最終コミット 2026-05-26（約7週間停滞）。参照先の Issue #31 は**本リポジトリに存在しない**（gh/REST とも 404。削除済みか別リポジトリ由来かは不明）。
- 判断: 7週間の間に chat-handler は別件（ペルソナ・レパートリー等）で動いており、handler.ts への統合コミット（7e133b6）はドリフト衝突のリスクが高い。**ブランチの直接マージ・rebase は捨てる**。資産価値があるのは純関数モジュール `queryExpansion.ts`（281行、handler 非依存）と knowledge.md 5件で、着手する時は**現 main から新ブランチを切って cherry-pick / 再適用**する。着手判断自体は家族体感系の優先度付け（07-07 診断の枠組み）に従う。
- ブランチは上記方針を決めた後も削除しない（cherry-pick 元として保持。移植完了時に削除）。

## 所見3（低）: Scheduler「矛盾」の正体は対象の書き分け不足

- todo.md:232 と :301 の「EventBridge → StartIngestionJob 自動実行ルール」（未完了 `[ ]`）は **S3 ObjectCreated トリガーの取り込み自動化**。todo.md:312 の「月次 EventBridge Scheduler 稼働開始」（完了 `[x]`）は **Ragas 評価の月次 cron**。別物であり完了状態の矛盾ではないが、本文に区別が書かれていないため「矛盾」に見えている。
- 解消手順（要 aws login）: `aws events list-rules`（または scheduler list-schedules）で実在リソースを確認 → 232/301 が同一物の重複記載なら1つに統合し、312 との違い（取り込み自動化 vs 月次評価）を1行ずつ明記する。

## 次の一手（優先順）

1. Scheduler 記述の書き分け修正（⏳てつてつ: aws login 後に実リソース確認とセット）
2. 家族体感系の着手判断（👎+自由記述 Issue #21 / クエリ拡張の再適用 / Phase3 LLM 補助分類 のどれを先にやるか）
3. Issue #1 は backlog 維持（優先度低と判断。閉じる場合は「やらない」判断としててつてつが決める）
